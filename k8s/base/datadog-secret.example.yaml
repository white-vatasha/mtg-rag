# Copy to datadog-secret.yaml, set your API key, then apply with the datadog overlay.
# Never commit a real API key.
apiVersion: v1
kind: Secret
metadata:
  name: datadog-secret
  namespace: mtg-rag
type: Opaque
stringData:
  api-key: "REPLACE_WITH_DATADOG_API_KEY"
